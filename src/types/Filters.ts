/**
 * Represents a band with a specific gain.
 * 
 * @interface Band
 * @property {number} band - The identifier for the band.
 * @property {number} gain - The gain value for the band.
 */
export interface Band {
	band: number;
	gain: number;
}
